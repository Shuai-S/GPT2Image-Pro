{
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "db:generate": "node -e \"console.error('本仓库使用手写幂等 SQL 迁移，禁止运行 drizzle-kit generate。'); process.exit(1);\"",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "lint": "pnpm --filter @repo/web exec biome lint ../../packages/database",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "nanoid": "^5.1.15",
    "pg": "^8.22.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "dotenv": "^16.5.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5"
  },
  "license": "AGPL-3.0-only"
}
