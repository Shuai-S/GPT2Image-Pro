---
title: 使用手册
description: NexaRelay IMAGE 使用手册：从注册、创作、参数选择到图库、历史、积分、外接 API 和账号设置。
---

这份手册面向第一次进入网站的用户，按真实使用路径说明每个入口能做什么、什么时候该选它、以及应该点哪里。

## 快速开始

1. 点击顶部导航的 **控制台**。如果还没登录，先按页面提示注册或登录。
2. 进入控制台后点击 **开始创作**，或在左侧菜单选择 **创作**。
3. 想直接生成图片，就在创作页顶部选择 **文生图** 或 **图生图**。
4. 想把多个提示词、参考图和生成结果放在同一个工作台里整理，就在左侧菜单选择 **无限画布**。
5. 先用默认参数生成一张图。确认流程跑通后，再调整模型、尺寸、质量、背景、后端分组等选项。
6. 生成结果可直接 **下载**，也会保存到 **图库** 和 **历史记录**。

## 先选哪个功能

| 你想做什么 | 选择哪个入口 | 怎么点 |
| --- | --- | --- |
| 只用文字生成图片 | **创作 → 文生图** | 输入提示词，选择 **单提示词**，点 **生成**。 |
| 一次生成多条不同提示词 | **创作 → 文生图** | 切到 **逐行批量**，每行写一个提示词，点 **生成逐行批量**。 |
| 改一张已有图片 | **创作 → 图生图** | 点 **上传图片**，写清楚要改哪里，点 **生成**。 |
| 局部重绘或替换元素 | **创作 → 图生图** | 上传源图后点 **绘制蒙版** 或 **上传蒙版**，保存蒙版后生成。 |
| 整理多个创意方向和参考图 | **无限画布** | 添加 **提示词**、**图片**、**生成** 节点，用 **连接** 串起来后点 **运行**。 |
| 做多步骤图片工作流 | **无限画布** | 把参考图连到生成节点，生成结果会作为新节点留在画布上。 |
| 管理成品图片 | **图库** | 查看已完成图片，下载或继续作为参考图使用。 |
| 查看失败、处理中和扣费明细 | **历史记录** | 打开记录查看状态、错误、积分消耗和参考图。 |
| 查看余额、购买积分或订阅 | **账单与用量** | 在 **用量** 看余额，在 **账单** 管理套餐。 |
| 给程序或第三方工具调用 | **外接 API** | 创建 API Key，再按 **系统文档** 调用接口。 |

## 创作页通用操作

进入 **创作** 后，页面左侧通常是输入区和结果区，右侧或顶部是参数区。不同模式会显示不同控件，但选择原则一致：

- **后端分组**：普通用户保持默认即可。只有你知道某个分组更适合某类任务时再切换；切换后，本次创作会按该分组的倍率计费。
- **图片模型**：默认模型适合大多数图片。需要特定模型风格时，再从模型下拉框切换。
- **尺寸**：不确定就选 **Auto**。做头像、商品图、海报、横幅时再改成对应比例或自定义尺寸。
- **质量**：草稿和试提示词选低或默认；最终交付图再提高质量。
- **背景**：需要透明 PNG、贴纸、Logo 素材时选择透明；普通照片、海报、插画选自动或不透明。
- **高清修复**：结果尺寸偏小或细节不够时开启；它可能更慢，也可能产生额外消耗。
- **分块修复**：图片文字、局部细节需要加强时再开；适合最终稿，不适合每次试稿都开。
- **提示词优化**：想让系统帮你补充细节时开启；如果必须严格保留原话，就关闭或选择尽量减少改写。

## 文生图

**适合**：从零生成头像、海报、插画、商品概念图、社交媒体配图。

使用步骤：

1. 进入 **创作 → 文生图**。
2. 在输入框写清楚主体、场景、风格、构图、颜色和用途。
3. 选择 **单提示词**。
4. 按需调整 **图片模型**、**尺寸**、**质量** 和 **背景**。
5. 点击 **生成**。
6. 结果满意就点 **下载**；想继续改，点 **继续编辑** 或去 **图生图** 上传结果。

提示词建议：

- 好的写法：主体 + 场景 + 风格 + 构图 + 光线 + 用途。
- 示例：`一张科技产品发布会主视觉，银色耳机悬浮在深色舞台中央，蓝白灯光，高级商业摄影，16:9 横幅。`
- 如果第一张不满意，优先改提示词里的主体和风格；最后再改高级参数。

## 逐行批量

**适合**：你已经有多条方向，想一次生成多张不同图。

使用步骤：

1. 进入 **创作 → 文生图**。
2. 切换到 **逐行批量**。
3. 每行输入一个完整提示词。
4. 查看页面上的 **提示词行数** 和 **总图片数**。
5. 设置模型、尺寸和质量后，点击 **生成逐行批量**。

选择建议：

- 每行代表一张图，不要把多个需求挤在同一行。
- 批量试风格时保持尺寸一致，方便对比。
- 积分会按实际生成张数消耗，生成前先看余额。

## 图生图

**适合**：保留原图主体，改风格、换背景、补细节、做局部重绘。

使用步骤：

1. 进入 **创作 → 图生图**。
2. 点击 **上传图片**，上传 PNG、JPEG 或 WebP。
3. 在文本框写明要怎么改，例如“保留人物姿势，把背景换成夜晚城市街道”。
4. 如需局部修改，点击 **绘制蒙版**，在第一张源图上涂出需要改的区域，然后点 **保存蒙版**。
5. 也可以点击 **上传蒙版**，上传一张 PNG 蒙版。
6. 设置尺寸。想尽量保持源图比例时，用自动或接近源图的比例。
7. 点击 **生成**。

蒙版选择：

- 只改局部：用 **绘制蒙版**，涂需要变化的区域。
- 已有专业遮罩：用 **上传蒙版**。
- 整张图重风格化：不要加蒙版，直接描述整体变化。

## 无限画布

**适合**：把多个提示词、参考图、生成步骤和结果放在同一个大画布里，做灵感整理、方案对比、连续改图和工作流拆解。

进入方式：

1. 登录后进入 **控制台**。
2. 在左侧菜单点击 **无限画布**。
3. 进入后会看到一个可拖动、可缩放的画布工作区。

顶部工具栏怎么选：

| 按钮 | 用途 | 什么时候点 |
| --- | --- | --- |
| **选择** | 选择、拖动、编辑节点 | 默认操作，移动节点或改节点内容时使用。 |
| **平移** | 拖动画布视角 | 画布内容很多，需要移动视野时使用。 |
| **连接** | 把一个节点输出连到另一个节点输入 | 需要让提示词、图片或生成结果作为下一步输入时使用。 |
| **提示词** | 新建文本提示词节点 | 先写创意、风格、要求或局部修改说明。 |
| **图片** | 上传图片节点 | 加入参考图、素材图或上一版图片。 |
| **生成** | 新建生成节点 | 需要按连接输入生成新图片时使用。 |
| **输出** | 新建空输出节点 | 临时占位或整理结果时使用。 |
| **运行** | 运行当前选中的提示词或生成节点 | 节点内容准备好后点它开始生成。 |
| **适配** | 自动把画布内容缩放到可见区域 | 找不到节点或画布太散时使用。 |
| **导入** | 导入画布 JSON | 恢复之前导出的画布。 |
| **导出** | 导出画布 JSON | 保存当前画布结构，方便以后继续编辑。 |
| **删除** | 删除选中的节点或连接线 | 清理不需要的内容。 |
| **清空** | 重置为空白画布 | 重新开始一个新画布。 |

基本操作：

1. 点击 **提示词**，在节点里输入你要生成的画面描述。
2. 需要参考图时，点击 **图片** 上传，或把图片文件直接拖到画布中。
3. 点击 **生成** 创建生成节点，在节点里可填写补充提示词、尺寸和模型。
4. 点击 **连接**，先点一个节点右侧的输出圆点，再点目标节点左侧的输入圆点。
5. 选中 **生成** 节点，点击节点上的运行按钮，或点击顶部 **运行**。
6. 生成完成后，结果会作为新的输出节点出现在画布上。
7. 想继续改图，就把输出结果连到新的 **生成** 节点，再写下一步修改要求。

节点怎么连接：

- **提示词 → 生成**：把文本要求交给生成节点。
- **图片 → 生成**：把图片作为参考图或待修改图片。
- **输出 → 生成**：把上一次结果继续作为下一步输入。
- 一个生成节点可以同时接收提示词和图片；系统会把它们合并成一次生成请求。

画布怎么移动和缩放：

- 选择 **平移** 后拖动画布，可以移动视角。
- 鼠标滚轮可缩放画布。
- 点击 **适配** 可快速回到能看到全部节点的视图。
- 右下角 **导航图** 会显示当前画布内容的位置和视野范围。

使用建议：

- 做单张简单图片：优先用 **文生图**，更快。
- 有多张参考图、多条提示词或多轮方案：用 **无限画布**。
- 每个方案单独建一组节点，方便对比不同方向。
- 重要画布及时点 **导出**，把 JSON 保存下来，后续可用 **导入** 恢复。

## 图库

**图库** 用来管理已经完成的图片。

常用标签：

- **成品**：最终完成并可下载的图片。
- **上传图**：你在图生图中上传过的参考图。

常用操作：

- 点击作品可预览大图。
- 使用 **下载** 保存到本地。
- 需要继续改图时，把作品作为参考图带回创作页。

## 历史记录

**历史记录** 会显示所有任务，包括成功、失败和处理中。

你可以在这里看：

- 任务状态。
- 使用的模型和尺寸。
- 消耗的积分。
- 失败原因。
- 参考图和提示词。

使用建议：

- 找不到刚才的结果：先看 **历史记录**，再看 **图库**。
- 任务失败：打开失败记录看错误原因，再决定是否重试。
- 对账积分：查看记录里的积分消耗明细。

## 账单与用量

在 **账单与用量** 可以管理套餐、积分和交易记录。

常用操作：

1. 打开 **账单与用量**。
2. 在 **用量** 查看可用积分、即将过期积分和交易记录。
3. 积分不足时，点击 **按量付费** 购买一次性积分，或点击 **查看订阅套餐** 升级套餐。
4. 在 **账单** 查看当前套餐、续费日期和账单历史。

选择建议：

- 偶尔创作：购买一次性积分。
- 经常创作：选择订阅套餐，获得每月积分和更多功能。
- 需要 API 或更高分辨率：根据定价页的套餐权益升级。

## 外接 API

**外接 API** 面向开发者或需要把生图能力接入其他工具的用户。

使用步骤：

1. 进入 **外接 API**。
2. 点击 **创建 API Key**。
3. 给 Key 命名，例如 `production` 或 `test`。
4. 可选填写 **积分限额**，限制这个 Key 最多消耗多少积分。
5. 可选选择 **审核级别** 和 **后端分组**。
6. 创建后立即复制 Key。它只完整显示一次。
7. 按 **[系统文档](/docs/system)** 里的 Base URL、鉴权和接口示例调用。

选择建议：

- 只是网页创作：不需要 API Key。
- 给脚本、工作流、第三方站点调用：创建 API Key。
- 担心误用或成本失控：一定设置积分限额。
- 多个项目共用账号：给每个项目单独创建 Key，方便撤销和追踪。

## 设置

在 **设置** 中管理账号和高级偏好。

常用区域：

- **账户**：修改姓名、头像等基本信息。
- **安全**：修改密码。
- **账单与用量**：查看套餐、积分和交易记录。
- **外接 API**：创建和管理 API Key。
- **后端**：选择网页端创作默认使用的系统后端账号池分组。
- **高级**：配置自定义 OpenAI 兼容 API 端点。

选择建议：

- 不懂后端分组：保持默认。
- 想网页端优先使用某个系统分组：到 **设置 → 后端** 修改。
- 有自己的 OpenAI 兼容 API，且不想消耗平台积分：到 **设置 → 高级** 启用自定义 API。
- 修改 API 后先点测试出图，确认可用再正式使用。

## 顶部导航和页面按钮

- **创作**：进入图片生成工作区。
- **无限画布**：进入节点式图片创作工作台。
- **定价**：查看套餐、积分、功能权益和升级入口。
- **文档**：打开本使用手册和高级 API 文档。
- **控制台**：查看余额、最近创作和常用入口。
- **语言图标**：切换界面语言。
- **月亮图标**：切换深色或浅色主题。
- **头像**：进入账号相关菜单。

## 常见选择

| 场景 | 推荐选择 |
| --- | --- |
| 第一次试用 | 文生图 + 默认参数 + 单提示词。 |
| 做最终海报 | 文生图，尺寸选目标比例，最终稿再开高清修复。 |
| 改商品背景 | 图生图，上传商品图，必要时用蒙版保护商品主体。 |
| 连续改同一张图 | 图生图，把上一张结果作为源图继续修改。 |
| 整理多套方案 | 无限画布，把提示词、参考图和结果按节点连接起来。 |
| 控制成本 | 默认质量、Auto 尺寸、少开修复、先小批量测试。 |
| 开发集成 | 外接 API，设置积分限额，读系统文档。 |

## 高级参考

- **[系统文档（含外部 API）](/docs/system)**：给开发者看的接口、鉴权、异步任务、积分和错误码说明。
